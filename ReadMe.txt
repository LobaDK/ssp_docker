Kommando til self-signed certificate:
dotnet dev-certs https -ep ~/ssp_keys/cert.pfx -t

Jeg kører docker-run med docker-run.yml, da det virker nemmere end at skrive det hele i terminalen
Kommando til Docker:
docker-compose up --build